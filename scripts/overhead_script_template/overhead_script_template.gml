function overhead_script_template()
	{
	var _args = argument[0];
	var _x = _args[@ 0];
	var _y = _args[@ 1];
	
	//Script template for player overhead scripts
	//Can be assigned to players with the code: callback_add(callback_overhead, overhead_script_template, CALLBACK_TYPE.permanent);
	}
/* Copyright 2024 Springroll Games / Yosi */